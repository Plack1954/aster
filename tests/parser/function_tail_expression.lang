struct Pair {
    first: long,
    second: long,
}

private long add(long first, long second) {
    return first + second;
}

private Pair MakePair(long first, long second) {
    return Pair { first, second };
}

int main() {
    var pair = MakePair(20, 22);
    Console.WriteLine(add(pair.first, pair.second));
    return 0;
}
