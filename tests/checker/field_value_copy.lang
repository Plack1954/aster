using System.Text;

struct Record {
    name: string,
}

private string ViewName(string value) {
    return value;
}

int main() {
    Record record = Record {
        name: "borrowed field",
    };
    Console.WriteLine(ViewName(record.name));
    return 0;
}
