bool Contains(List<string> values, string wanted)
{
    foreach (string value in values)
    {
        if (value == wanted) { return true; }
    }
    return false;
}

void AppendAfterRead(ref List<string> values)
{
    int found = if (Contains(values, "missing")) { 1 } else { 0 };
    print(found);
    values.Add("added");
}

int main()
{
    List<string> values = new();
    AppendAfterRead(values);
    print(values.Count);
    return 0;
}
