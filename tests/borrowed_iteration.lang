int main() {
    List<long> values = new();
    values.Add(3);
    values.Add(5);
    long total = 0;
    foreach (long value in values) {
        total += value;
    }
    print(total);
    print(values.Count);
    long fixed[3] = [2, 4, 6];
    long fixedTotal = 0;
    foreach (long item in fixed) {
        fixedTotal += item;
    }
    print(fixedTotal);
    print(fixed[1]);
    return 0;
}
