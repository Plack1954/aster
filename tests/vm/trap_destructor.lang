struct Tracked {
    id: long,
}

~Tracked() {
    Console.WriteLine(self.id);
}

int main() {
    Tracked tracked = Tracked { id: 9 };
    long zero = 0;
    Console.WriteLine(1 / zero);
    return 0;
}
