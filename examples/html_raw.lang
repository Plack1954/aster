using Aster.Html;

int main() {
    Html trusted = Html.UnsafeRaw("<em>trusted</em>");
    Html document = <section>{trusted}</section>;
    string output = HtmlRender(document);
    print(output);
    return 0;
}
